# DocuLens — Python dependencies
# Install with: pip install -r requirements.txt
#
# Windows note: pytesseract also needs Tesseract binary installed separately.
# Download from: https://github.com/UB-Mannheim/tesseract/wiki
# Add to PATH: C:\Program Files\Tesseract-OCR\

# ── PDF & OCR ─────────────────────────────────────────────────────────────────
pymupdf>=1.24.0          # PyMuPDF — fast PDF text extraction (pip name is pymupdf)
pdf2image>=1.17.0        # Render PDF pages to images for OCR
pytesseract>=0.3.13      # Python wrapper for Tesseract OCR
Pillow>=10.0.0           # Image processing (required by pdf2image + pytesseract)

# ── Document generation (for test data) ──────────────────────────────────────
reportlab>=4.1.0         # Generate synthetic German PDF invoices

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0        # Coverage reports

# ── Coming in Layer 2 (embeddings + vector store) ────────────────────────────
# sentence-transformers>=3.0.0
# faiss-cpu>=1.8.0
# langchain>=0.2.0
# langchain-community>=0.2.0

# ── Coming in Layer 3 (LLM extraction) ───────────────────────────────────────
# langchain-groq>=0.1.0
# pydantic>=2.7.0

# ── Coming in Layer 5 (API + UI) ─────────────────────────────────────────────
# fastapi>=0.111.0
# uvicorn>=0.30.0
# streamlit>=1.36.0
# python-multipart>=0.0.9
